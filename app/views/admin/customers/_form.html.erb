<!--<#%= path = if @customer.new_record? ? new_admin_customer_path : edit_admin_customer_path %>-->

<%= form_for [:admin, @customer], url: admin_customers_path, html: { :'data-remote' => true } do |f| %>
  <% if @customer.errors.any? %>
    <div id="error_explanation">
      <ul>
      <% @customer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= f.label :name, 'Name :' %><br />
    <%= f.text_field :name %>
  </div>

  <div>
    <%= f.label :email, 'Email :' %><br />
    <%= f.text_field :email %>
  </div>

  <div>
    <%= f.label :phone_number, 'Phone Number:' %><br />
    <%= f.text_field :phone_number %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>